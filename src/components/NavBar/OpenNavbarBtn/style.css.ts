import { style } from '@vanilla-extract/css';
import { themeVars } from '../../../themes.css';

export const openNavbarBtnClass = style({
  width: '26px',
  height: '26px',
  color: themeVars.controls,
});
