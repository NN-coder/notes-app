import { style } from '@vanilla-extract/css';

export const dateOfChangeClass = style({
  position: 'fixed',
  bottom: '15px',
  left: '50%',
  transform: 'translateX(-50%)',
  fontSize: '1.2rem',
});
